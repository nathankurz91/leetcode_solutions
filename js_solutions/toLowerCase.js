/**
 * @param {string} str
 * @return {string}
 * 
 * There is already a function in js that turns a string to all lowercase
 * I should rewrite a custom one that uses ascii
 */
var toLowerCase = function(str) {
    return str.toLowerCase();
};